# name: Keep App Alive

# on:
#   schedule:
#     - cron: '*/5 * * * *'  # Runs every 5 minutes

# jobs:
#   keep-alive:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Ping Sellstream App
#         run: |
#           curl -X GET https://sellstreams1.onrender.com/doc/
#           curl -X GET https://quiz-application-vzaz.onrender.com/
#           curl -X GET https://pet-adoption-bd.onrender.com/
